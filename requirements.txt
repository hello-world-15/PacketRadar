# Requirements for the Streamlit deployment (PCAP Analyzer only).
# This is intentionally a subset of backend/requirements.txt — it omits
# fastapi/uvicorn/python-multipart/psutil, which are only needed by the
# live-capture FastAPI server, not by this Streamlit app.
streamlit>=1.38
scapy>=2.5
pydantic>=2.9
reportlab>=4.0
matplotlib>=3.8
pandas>=2.2
